/**
 * Canvas configuration constants
 * These values define the virtual canvas size and default zoom level
 */

// Virtual canvas dimensions - creates a large workspace for collaboration
export const CANVAS_WIDTH = 4000;
export const CANVAS_HEIGHT = 4000;

// Default zoom level when canvas loads
export const INITIAL_ZOOM = 1;

// Zoom limits to prevent extreme zoom levels
export const MIN_ZOOM = 0.1;
export const MAX_ZOOM = 5.0;

// Canvas interaction constants
export const ZOOM_STEP = 0.1;
export const PAN_SPEED = 1;
